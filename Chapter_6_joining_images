import cv2

import numpy as np

img = cv2.imread('Resources/lena.png')

imghor = np.hstack((img,img))

imgver = np.vstack((img,img))

cv2.imshow('vertical lena',imgver)
cv2.imshow('horizontal lena',imghor)
cv2.waitKey(0)